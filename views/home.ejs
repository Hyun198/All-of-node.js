<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <title>Home</title>
  </head>
  <body>
    <% if (loggedInUser) { %>
    <h1>Home</h1>
    <h2>안녕하세요, <%= loggedInUser.username %>님!</h2>
    <form action="/logout" method="POST">
      <button type="submit">로그아웃</button>
    </form>
    <ul>
      <li>
        <a href="/profile/<%=loggedInUser.id %>"
          >프로필 페이지(로그인해야만 가능)</a
        >
      </li>
      <li><a href="/cgv">cgv 시간 계산</a></li>
      <li><a href="/posts">전체 게시판</a></li>
      <li><a href="/createPost">글쓰기</a></li>
    </ul>

    <ul>
      <% users.forEach(user =>{ %>
      <li>
        <p>사용자명: <%= user.username %></p>
        <p>생년월일: <%= user.birthdate.toDateString() %></p>
        <a href="/profile/<%=user._id %>">
          <img src="/profile-image/<%=user.id%>" alt="프로필사진" />
        </a>
      </li>

      <% }) %>
    </ul>

    <% } else { %>
    <h1>Home</h1>
    <ul>
      <li><a href="/signup">Sign up</a></li>
      <li><a href="/login">Login</a></li>
      <li><a href="/profile">보호된 페이지(로그인해야만 가능)</a></li>
      <li><a href="/cgv">cgv 시간 계산</a></li>
      <li><a href="/users">가입된 유저들 목록</a></li>
    </ul>
    <h2>전체 게시판</h2>
    <ul>
      <% posts.forEach(post =>{ %>
      <li>
        제목:
        <a href="/posts/<%=post._id %>"> <%=post.title%> </a>
        <p>본문: <%=truncateText(post.desc, 100) %></p>
        <p>작성자: <%=post.author %></p>
        <p>작성날짜: <%=post.updatedAt.toDateString()%></p>
      </li>

      <% }) %>
    </ul>
    <% } %> <% function truncateText(text, maxLength) { if (text.length >
    maxLength) { return text.slice(0, maxLength) + '...'; } return text; } %>
  </body>
</html>
